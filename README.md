# markets

### Initialize Nasdaq data
run initialize_ticker_cache.py

This will create a cache folder and generate a list of NASDAQ ticker symbols and generate a .csv for each ticker with historical data in the cache/tickers directory